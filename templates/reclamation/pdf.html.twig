<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Réclamation #{{ reclamation.id }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .logo {
            max-width: 150px;
            margin-bottom: 20px;
        }
        .title {
            color: #3498db;
            font-size: 24px;
            margin-bottom: 10px;
        }
        .subtitle {
            font-size: 18px;
            color: #666;
            margin-bottom: 30px;
        }
        .info-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }
        .info-table th {
            background-color: #3498db;
            color: white;
            padding: 10px;
            text-align: left;
        }
        .info-table td {
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }
        .description {
            margin-bottom: 30px;
        }
        .response {
            background-color: #f8f9fa;
            padding: 15px;
            margin-top: 20px;
            border-radius: 5px;
        }
        .footer {
            margin-top: 50px;
            text-align: right;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="{{ absolute_url(asset('images/logo.png')) }}" alt="Logo" class="logo">
        <h1 class="title">E-Sportify</h1>
        <h2 class="subtitle">Détails de la Réclamation #{{ reclamation.id }}</h2>
    </div>

    <table class="info-table">
        <tr>
            <th>Description</th>
            <td>{{ reclamation.description }}</td>
        </tr>
        <tr>
            <th>Statut</th>
            <td>{{ reclamation.statut }}</td>
        </tr>
        <tr>
            <th>Date de création</th>
            <td>{{ reclamation.date_creation|date('d/m/Y H:i') }}</td>
        </tr>
        {% if reclamation.categorie %}
        <tr>
            <th>Catégorie</th>
            <td>{{ reclamation.categorie }}</td>
        </tr>
        {% endif %}
        <tr>
            <th>État</th>
            <td>{{ reclamation.archived ? 'Archivée' : 'Active' }}</td>
        </tr>
    </table>

    {% if reclamation.reponse %}
    <div class="response">
        <h3>Réponse :</h3>
        <p>{{ reclamation.reponse }}</p>
    </div>
    {% endif %}

    <div class="footer">
        Document généré le {{ "now"|date('d/m/Y H:i') }}
    </div>
</body>
</html> 