<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Utilisateurs</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
        }
        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 30px;
        }
        .user-list {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 40px;
        }
        .user-list th, .user-list td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }
        .user-list th {
            background-color: #f5f5f5;
            font-weight: bold;
        }
        .user-photo {
            width: 50px;
            height: 50px;
            border-radius: 50%;
        }
        .stats-container {
            margin-top: 40px;
            text-align: center;
        }
        .stats-title {
            font-size: 18px;
            color: #333;
            margin-bottom: 20px;
        }
        .stats-grid {
            display: flex;
            justify-content: space-around;
            margin-bottom: 30px;
        }
        .stat-item {
            text-align: center;
            padding: 15px;
            border-radius: 8px;
            background-color: #f8f9fa;
            min-width: 150px;
        }
        .stat-number {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .stat-label {
            color: #666;
        }
        .role-badge {
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 14px;
            font-weight: bold;
        }
        .role-joueur {
            background-color: #36a2eb;
            color: white;
        }
        .role-vendeur {
            background-color: #ffcd56;
            color: black;
        }
        .role-organisateur {
            background-color: #4bc0c0;
            color: white;
        }
    </style>
</head>
<body>
    <h1>Liste des Utilisateurs E-Sportify</h1>
    
    <div class="stats-container">
        <h2 class="stats-title">Statistiques des Utilisateurs</h2>
        <div class="stats-grid">
            <div class="stat-item">
                <div class="stat-number" style="color: #36a2eb;">{{ stats.joueurs }}</div>
                <div class="stat-label">Joueurs</div>
            </div>
            <div class="stat-item">
                <div class="stat-number" style="color: #ffcd56;">{{ stats.vendeurs }}</div>
                <div class="stat-label">Vendeurs</div>
            </div>
            <div class="stat-item">
                <div class="stat-number" style="color: #4bc0c0;">{{ stats.organisateurs }}</div>
                <div class="stat-label">Organisateurs</div>
            </div>
        </div>
    </div>

    <table class="user-list">
        <thead>
            <tr>
                <th>Photo</th>
                <th>Nom Complet</th>
                <th>Email</th>
                <th>Rôle</th>
            </tr>
        </thead>
        <tbody>
            {% for utilisateur in utilisateurs %}
            <tr>
                <td>
                    <img src="{{ absolute_url(asset(utilisateur.photo)) }}" alt="Photo de profil" class="user-photo">
                </td>
                <td>{{ utilisateur.nom }} {{ utilisateur.prenom }}</td>
                <td>{{ utilisateur.email }}</td>
                <td>
                    <span class="role-badge role-{{ utilisateur.role }}">
                        {{ utilisateur.role|capitalize }}
                    </span>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html> 