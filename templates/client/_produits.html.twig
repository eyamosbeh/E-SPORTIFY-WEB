<h2>Nos Produits</h2>
<p>Ici s'affichera la liste des produits (à intégrer avec le vrai code plus tard).</p>

<div class="row g-4 justify-content-center">
    {% for produit in produits %}
        <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <div class="card h-100 shadow-sm border-0" style="border-radius:22px;box-shadow:0 4px 18px rgba(33,147,176,0.13);">
                <div class="card-img-top d-flex align-items-center justify-content-center" style="height:180px;background:linear-gradient(90deg,#2193b0 0%,#6dd5ed 100%);border-top-left-radius:22px;border-top-right-radius:22px;">
                    {% if produit.image %}
                        <img src="{{ asset(produit.image) }}" alt="{{ produit.nom }}" style="max-height:160px;max-width:100%;object-fit:contain;">
                    {% else %}
                        <span class="text-white">No image</span>
                    {% endif %}
                </div>
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title mb-1" style="font-weight:bold;color:#2193b0;">{{ produit.nom }}</h5>
                    <p class="card-text small text-muted mb-2">{{ produit.description|slice(0, 60) ~ (produit.description|length > 60 ? '...' : '') }}</p>
                    <div class="fw-bold mb-2" style="color:#43cea2;font-size:1.2rem;">{{ produit.prix|number_format(2, '.', ' ') }} TND</div>
                    <form method="post" action="{{ path('ajouter_au_panier', {'id': produit.id}) }}" class="mt-auto">
                        <button type="submit" class="btn w-100" style="border-radius:22px;background:linear-gradient(90deg,#2193b0 0%,#6dd5ed 100%);color:#fff;font-weight:600;box-shadow:0 2px 8px rgba(33,147,176,0.07);transition:background 0.2s;">Ajouter au panier</button>
                    </form>
                </div>
            </div>
        </div>
    {% else %}
        <div class="col-12 text-center text-muted">Aucun produit disponible.</div>
    {% endfor %}
</div> 