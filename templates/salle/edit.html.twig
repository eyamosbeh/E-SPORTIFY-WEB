{% extends 'base.html.twig' %}

{% block title %}Modifier la Salle{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h1 class="mb-0">Modifier la salle</h1>
            {{ include('salle/_delete_form.html.twig') }}
        </div>
        <div class="card-body">
            {{ include('salle/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
        <div class="card-footer">
            <a href="{{ path('app_salle_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
    </div>
{% endblock %} 