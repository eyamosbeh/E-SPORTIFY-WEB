<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}Gestion des Réclamations{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <!-- Bootstrap 5 CSS -->
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
            <!-- Font Awesome -->
            <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
            <!-- Google Fonts - Unigine Theme -->
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Oxanium:wght@400;500;600;700&family=Work+Sans:wght@600&display=swap" rel="stylesheet">
            <!-- Global CSS -->
            <link href="{{ asset('assets/css/style.css') }}" rel="stylesheet">
            <!-- Reclamation CSS -->
            <link href="{{ asset('assets/css/reclamation.css') }}" rel="stylesheet">
            <style>
                /* Unigine Theme CSS */
                :root {
                    --bg-purple: hsla(267, 100%, 63%, 1);
                    --bg-purple-alpha-30: hsla(267, 100%, 63%, 0.3);
                    --bg-dark-purple: hsla(279, 42%, 9%, 1);
                    --bg-oxford-blue: hsla(240, 63%, 13%, 1);
                    --bg-oxford-blue-alpha-95: hsla(240, 63%, 13%, 0.95);
                    --bg-oxford-blue-alpha-90: hsla(240, 63%, 13%, 0.9);
                    --bg-oxford-blue-alpha-80: hsla(240, 63%, 13%, 0.8);

                    --gradient-1: linear-gradient(to right bottom, hsl(299, 100%, 52%), hsl(291, 100%, 58%), hsl(283, 100%, 60%), hsl(273, 100%, 62%), hsl(262, 100%, 63%), hsl(242, 100%, 69%), hsl(223, 100%, 62%), hsl(210, 100%, 50%), hsl(203, 100%, 50%), hsl(198, 100%, 50%), hsl(192, 100%, 48%), hsl(185, 90%, 48%));
                    --gradient-2: linear-gradient(90deg, transparent 0%, #9841ff 50%, transparent 100%);

                    --text-white: hsla(0, 0%, 100%, 1);
                    --text-gainsboro: hsla(0, 0%, 87%, 1);
                    --text-champagne-pink: hsla(23, 61%, 86%, 1);
                    --text-purple: hsla(267, 100%, 63%, 1);

                    --border-space-cadet: hsl(240, 45%, 17%);
                    --border-purple-alpha-30: hsla(267, 100%, 63%, 0.3);

                    --fontFamily-oxanium: 'Oxanium', cursive;
                    --fontFamily-work-sans: 'Work Sans', sans-serif;

                    --fontSize-1: 3.6rem;
                    --fontSize-2: 2.4rem;
                    --fontSize-3: 2rem;
                    --fontSize-4: 1.7rem;
                    --fontSize-5: 1.6rem;
                    --fontSize-6: 1.5rem;
                    --fontSize-7: 1.4rem;
                    --fontSize-8: 1.2rem;

                    --weight-regular: 400;
                    --weight-semiBold: 600;
                    --weight-bold: 700;

                    --section-spacing: 60px;

                    --shadow: 0px 2px 5px 0px hsla(0, 0%, 0%, 0.2);

                    --radius-circle: 50%;
                    --radius-5: 5px;
                    --radius-3: 3px;

                    --clip-path-1: polygon(0 0, 100% 0, 100% 0, 0 0);
                    --clip-path-2: polygon(0 0, 100% 0, 100% 100%, 0 100%);
                    --clip-path-3: polygon(0% 0%, 90% 0, 100% 30%, 100% 100%, 0 100%);
                    --clip-path-4: polygon(90% 0, 100% 40%, 100% 100%, 0 100%, 0 0);
                    --clip-path-5: polygon(100% 0, 100% 100%, 10% 100%, 0 60%, 0 0);
                    --clip-path-6: polygon(10% 0, 100% 0, 100% 100%, 0 100%, 0 40%);
                    --clip-path-7: polygon(100% 0, 100% 60%, 90% 100%, 0 100%, 0 0);
                    --clip-path-8: polygon(0% 0%, 70% 0, 100% 30%, 100% 100%, 0 100%);
                    --clip-path-9: polygon(0 0, 90% 0, 100% 100%, 0% 100%);

                    --transition-1: 250ms ease;
                    --transition-2: 500ms ease;
                    --cubic-out: cubic-bezier(0.05, 0.83, 0.52, 0.97);
                }

                body {
                    background-color: #1A0D21;
                    color: var(--text-gainsboro);
                    font-family: var(--fontFamily-oxanium);
                    font-size: var(--fontSize-8);
                    line-height: 1.8;
                }

                html, body {
                    background-color: #1A0D21 !important;
                    min-height: 100vh;
                    margin: 0;
                    padding: 0;
                }

                .card {
                    background-color: rgba(26, 13, 33, 0.8) !important;
                }

                .container {
                    background-color: #1A0D21 !important;
                }

                .header {
                    position: fixed;
                    top: 0;
                    left: 0;
                    width: 100%;
                    background-color: hsla(240, 63%, 13%, 0.9);
                    padding-block: 25px;
                    z-index: 4;
                    border-bottom: 1px solid var(--border-space-cadet);
                }
                
                .header.active {
                    padding-block: 20px;
                    background-color: hsla(240, 63%, 13%, 0.95);
                    box-shadow: var(--shadow);
                }
                
                .header .container {
                    display: flex;
                    justify-content: space-between;
                    align-items: center;
                    gap: 30px;
                }
                
                .header-actions {
                    display: flex;
                    align-items: center;
                    gap: 20px;
                    margin-left: auto;
                }
                
                .header .logo img { width: 100%; }
                
                .navbar {
                    position: relative;
                    background: transparent;
                    padding: 0;
                }
                
                .navbar-list {
                    display: flex;
                    gap: 20px;
                    padding-left: 0;
                    list-style: none;
                    margin-bottom: 0;
                }
                
                .navbar-item {
                    position: relative;
                }
                
                .navbar-link {
                    color: var(--text-white);
                    font-size: var(--fontSize-7);
                    font-weight: var(--weight-semiBold);
                    text-transform: uppercase;
                    padding: 10px;
                    transition: var(--transition-1);
                    text-decoration: none;
                }
                
                .navbar-link:hover {
                    color: var(--text-purple);
                }

                .container {
                    padding-inline: 15px;
                }

                .btn {
                    color: var(--text-white);
                    font-size: var(--fontSize-8);
                    font-weight: var(--weight-semiBold);
                    text-transform: uppercase;
                    max-width: max-content;
                    padding: 15px 25px;
                    background-image: var(--gradient-1);
                    background-size: 200%;
                    background-position: left;
                    transition: var(--transition-2);
                    border: none;
                    border-radius: 0;
                    clip-path: var(--clip-path-3);
                }
                
                .btn:hover, .btn:focus {
                    background-position: right;
                    color: var(--text-white);
                }

                .card {
                    background-color: var(--bg-oxford-blue);
                    border-radius: var(--radius-5);
                    border: 1px solid var(--border-space-cadet);
                    box-shadow: var(--shadow);
                    margin-top: 30px;
                }
                
                .card-header {
                    background-color: var(--bg-oxford-blue-alpha-90);
                    border-bottom: 1px solid var(--border-space-cadet);
                    color: var(--text-white);
                    padding: 15px 20px;
                }
                
                .card-body {
                    padding: 20px;
                }

                .title, .h1, .h2, .h3, .h4, .h5, .h6 {
                    color: var(--text-white);
                    font-family: var(--fontFamily-oxanium);
                    font-weight: var(--weight-bold);
                    text-transform: uppercase;
                    line-height: 1.2;
                }

                .page-content {
                    padding-top: 120px;
                    padding-bottom: 60px;
                    min-height: calc(100vh - 80px);
                }

                .breadcrumb {
                    background-color: var(--bg-oxford-blue-alpha-80);
                    padding: 15px;
                    border-radius: var(--radius-5);
                    margin-bottom: 30px;
                }

                .breadcrumb-item a {
                    color: var(--text-champagne-pink);
                    text-decoration: none;
                }

                .breadcrumb-item.active {
                    color: var(--text-white);
                }


                .form-control, .form-select {
                    background-color: var(--bg-oxford-blue-alpha-80);
                    border: 1px solid var(--border-space-cadet);
                    color: var(--text-white);
                    padding: 12px 15px;
                    border-radius: var(--radius-5);
                }

                .form-control:focus, .form-select:focus {
                    background-color: var(--bg-oxford-blue);
                    border-color: var(--bg-purple);
                    box-shadow: 0 0 0 0.25rem var(--bg-purple-alpha-30);
                    color: var(--text-white);
                }

                .form-label {
                    color: var(--text-white);
                    margin-bottom: 8px;
                }

                .table {
                    color: var(--text-gainsboro);
                }

                .table thead th {
                    background-color: var(--bg-oxford-blue-alpha-90);
                    color: var(--text-white);
                    border-color: var(--border-space-cadet);
                }

                .alert {
                    border-radius: var(--radius-5);
                    border: none;
                }

                /* Styles pour les boutons Contact Us */
                .contact-btn {
                    display: inline-block;
                    background: linear-gradient(90deg, #9841f2, #5f3dc8);
                    color: white;
                    padding: 10px 20px;
                    border-radius: 50px;
                    font-weight: bold;
                    margin-right: 15px;
                    transition: all 0.3s ease;
                    text-decoration: none;
                }
                
                .contact-btn:hover {
                    transform: scale(1.05);
                    box-shadow: 0 0 15px rgba(152, 65, 242, 0.5);
                    color: white;
                    text-decoration: none;
                }

                /* Adaptations pour les éléments Bootstrap */
                .btn-primary {
                    background-image: var(--gradient-1);
                    background-size: 200%;
                    background-position: left;
                    border: none;
                }
                
                .btn-primary:hover {
                    background-position: right;
                }
                
                .btn-success {
                    background-color: #28a745;
                    border-color: #28a745;
                }
                
                .btn-danger {
                    background-color: #dc3545;
                    border-color: #dc3545;
                }
                
                .btn-warning {
                    background-color: #ffc107;
                    border-color: #ffc107;
                }
                
                .btn-secondary {
                    background-color: var(--bg-oxford-blue);
                    border-color: var(--border-space-cadet);
                }

                .modal-content {
                    background-color: var(--bg-oxford-blue);
                    border: 1px solid var(--border-space-cadet);
                    color: var(--text-gainsboro);
                }
                
                .modal-header {
                    border-bottom: 1px solid var(--border-space-cadet);
                }
                

                @media (max-width: 991px) {
                    .navbar-collapse {
                        background-color: var(--bg-oxford-blue);
                        padding: 15px;
                        border-radius: var(--radius-5);
                    }
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            <!-- Bootstrap Bundle with Popper -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
            <!-- Font Awesome -->
            <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/js/all.min.js"></script>
            {% block importmap %}{{ importmap('app') }}{% endblock %}
            <!-- Ionicons (pour le design Unigine) -->
            <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
            <script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>
        {% endblock %}
    </head>
    <body>
        <!-- Flash messages -->
        <div class="page-content">
            <div class="container mt-4">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} alert-dismissible fade show animate-fade-in" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>
            
            <!-- Main content -->
            <main>
                {% block body %}{% endblock %}
            </main>
        </div>
        
        
    </body>
</html>
