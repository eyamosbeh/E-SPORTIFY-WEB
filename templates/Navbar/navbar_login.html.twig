{# Remixicons #}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.5.0/remixicon.css">

{# CSS principal #}
<link rel="stylesheet" href="{{ asset('navbar/css/styles.css') }}">

{# SCSS compilé #}
<link rel="stylesheet" href="{{ asset('navbar/scss/main.scss') }}">

<!--==================== HEADER ====================-->
<header class="nav-header {% if is_profile is defined and is_profile %}profile-nav{% endif %}">
    <nav class="nav-navigation container">
        <a href="{{ path('app_home') }}" class="nav-navigation__logo">
            <img src="{{ asset('navbar/img/E-SPORTIFY.png') }}" alt="E-SPORTIFY">
        </a>

        <div class="nav-navigation__menu" id="nav-menu">
            <ul class="nav-navigation__list">
                <li>
                    <a href="{{ path('app_home') }}" class="nav-navigation__link">Accueil</a>
                </li>

                <li>
                    <a href="#" class="nav-navigation__link">À propos</a>
                </li>

                <li>
                    <a href="{{ path('app_events_cards') }}" class="nav-navigation__link">Events</a>
                </li>

                <li>
                    <a href="#" class="nav-navigation__link">Produits</a>
                </li>

                <li>
                    <a href="{{ path('app_reclamation_index') }}" class="nav-navigation__link">Contact</a>
                </li>
                <li>
                    <a href="{{ path('app_post_index') }}" class="nav-navigation__link">Posts</a>
                </li>

            </ul>

            <!-- Close button -->
            <div class="nav-navigation__close" id="nav-close">
                <i class="ri-close-line"></i>
            </div>
        </div>

        <div class="nav-navigation__actions">
            <!-- Dropdown -->
            <div class="dropdown" id="dropdown">
                <div class="dropdown__profile">
                    <div class="dropdown__names">
                        <h3>{{ app.session.get('user').prenom }} {{ app.session.get('user').nom }}</h3>
                        <span>{{ app.session.get('user').role }}</span>
                    </div>

                    <div class="dropdown__image">
                        <img src="{{ asset(app.session.get('user').photo ?? 'Sign_in/img/user.png') }}" alt="Profile">
                    </div>
                </div>

                <div class="dropdown__list">
                    <a href="{{ path('app_profile') }}" class="dropdown__link">
                        <i class="ri-user-line"></i>
                        <span>Profile</span>
                    </a>

                   

                    

                    <a href="{{ path('app_profile_settings') }}" class="dropdown__link">
                        <i class="ri-settings-3-line"></i>
                        <span>Paramètres</span>
                    </a>

                    <a href="{{ path('app_logout') }}" class="dropdown__link">
                        <i class="ri-logout-box-r-line"></i>
                        <span>Déconnexion</span>
                    </a>
                </div>
            </div>

            <!-- Toggle button -->
            <div class="nav-navigation__toggle" id="nav-toggle">
                <i class="ri-menu-line"></i>
            </div>
        </div>
    </nav>
</header>

<script src="{{ asset('navbar/js/main.js') }}" defer></script>