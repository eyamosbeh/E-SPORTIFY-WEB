<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Réservations</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #333;
            margin-bottom: 5px;
        }
        .date {
            font-size: 14px;
            color: #666;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .status {
            padding: 3px 6px;
            border-radius: 3px;
            font-weight: bold;
            font-size: 12px;
        }
        .status-confirmed {
            background-color: #d4edda;
            color: #155724;
        }
        .status-pending {
            background-color: #fff3cd;
            color: #856404;
        }
        .status-canceled {
            background-color: #f8d7da;
            color: #721c24;
        }
        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Liste des Réservations</h1>
        <div class="date">Généré le {{ date|date('d/m/Y à H:i') }}</div>
    </div>
    
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Salle</th>
                <th>Date de début</th>
                <th>Date de fin</th>
                <th>Statut</th>
            </tr>
        </thead>
        <tbody>
            {% for reservation in reservations %}
                <tr>
                    <td>{{ reservation.id }}</td>
                    <td>{{ reservation.salle.nom }}</td>
                    <td>{{ reservation.dateDebut|date('d/m/Y H:i') }}</td>
                    <td>{{ reservation.dateFin|date('d/m/Y H:i') }}</td>
                    <td>
                        <span class="status {% if reservation.statut == 'Confirmée' %}status-confirmed{% elseif reservation.statut == 'En attente' %}status-pending{% else %}status-canceled{% endif %}">
                            {{ reservation.statut }}
                        </span>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">Aucune réservation trouvée</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="footer">
        <p>© {{ 'now'|date('Y') }} - Système de Gestion des Réservations</p>
    </div>
</body>
</html>
